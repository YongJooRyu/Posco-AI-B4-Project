// src/theme.js
export const theme = {

  // ── 배경색 ──────────────────────────
  bgMain:    "#e8f4ff",   // 전체 배경 (연한 하늘)
  bgCard:    "#ffffff",   // 카드 배경
  bgBar:     "#d0e8ff",   // 상단 바
  bgInput:   "#f0f8ff",   // 입력창
  bgButton:  "#5b9bd5",   // 기본 버튼
  bgHover:   "#4a8ac4",   // 버튼 hover

  // ── 텍스트 ──────────────────────────
  textMain:  "#2a4a6a",   // 기본 텍스트
  textSub:   "#7a9ab8",   // 보조 텍스트
  textMuted: "#aac4d8",   // 흐린 텍스트
  textBtn:   "#ffffff",   // 버튼 텍스트

  // ── 강조색 ──────────────────────────
  primary:   "#5b9bd5",   // 메인 파랑
  primaryLight: "#a8d0f0", // 연한 파랑
  success:   "#7ec8a0",   // 집중 초록
  warning:   "#ffcc88",   // 미집중 노랑
  danger:    "#ff9999",   // 졸음 빨강
  accent:    "#b8a0e8",   // 포인트 보라

  // ── 테두리 ──────────────────────────
  border:        "#b8d8f0",   // 기본 테두리
  borderStrong:  "#5b9bd5",   // 강조 테두리
  borderWidth:   "3px",       // 픽셀 느낌 두께

  // ── 모양 ────────────────────────────
  radius:    0,           // 픽셀 느낌 (각지게)
  radiusSm:  4,           // 살짝만 둥글게

  // ── 픽셀 그림자 ─────────────────────
  shadow:    "3px 3px 0px #b8d8f0",
  shadowBtn: "3px 3px 0px #4a8ac4",

  // ── 폰트 ────────────────────────────
  font:      "'Press Start 2P', 'Noto Sans KR', monospace",
  fontBody:  "'Noto Sans KR', sans-serif",
}